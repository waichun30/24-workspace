#hello


build Spring-boot backend
1) mvn clean install
2) cd to target folder
3) gcloud app deploy <name.jar>

#Frontend
Using Angula framework, located at src/webapp

to run local, 

1) ng build
2) gcloud app deploy

